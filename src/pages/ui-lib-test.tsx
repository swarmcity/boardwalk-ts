import { Button } from '@swarm-city/ui-library'
import { RouteComponentProps } from '@reach/router'

type UiLibTestProps = RouteComponentProps

export const UiLibTest = (_: UiLibTestProps) => <Button>Test</Button>
